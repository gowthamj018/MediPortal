# ============================================================
# DEV profile — used when SPRING_PROFILES_ACTIVE=dev
# SMS OTP is mocked (logged to console only).
# Email OTP is always sent via real SMTP — check .env for credentials.
# ============================================================

# SMS — always mock in dev
app.sms.provider=mock

# Show SQL in dev for easier debugging
spring.jpa.show-sql=true
